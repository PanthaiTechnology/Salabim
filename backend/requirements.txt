fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.9
redis==5.0.8
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
boto3==1.35.24
tenacity==9.0.0
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1

# Transcrição de voz (modo Cantar) — roda local, sem chave/custo. Também
# exige o binário do ffmpeg instalado no sistema (não é um pacote pip).
openai-whisper==20250625

# Correção ortográfica pra busca por texto (tolerar erro de digitação) — roda local, sem chave/custo.
pyspellchecker==0.8.1

# Normalização de ganho do áudio do modo Ouvir antes do fingerprint (ver
# app/services/audio_utils.py) — usa o mesmo binário do ffmpeg que o Whisper
# já exige no sistema, não precisa instalar nada além do pacote Python.
pydub==0.25.1
